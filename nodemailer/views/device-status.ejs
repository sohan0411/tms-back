<!DOCTYPE html>
<html>
<head>
  <title>Device Status Update</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f2f2f2;
      color: #333;
    }

    h1 {
      color: #008080;
    }

    ul {
      list-style: none;
      padding: 0;
    }

    li {
      border: 1px solid #ccc;
      padding: 10px;
      margin: 10px 0;
      background-color: #fff;
    }

    strong {
      color: #006400;
    }

    em {
      color: #800000;
      font-style: italic;
    }
  </style>
</head>
<body>
  <h1>Device Status Update</h1>
  <ul>
    <% for(let i = 0; i < devices.length; i++) { %>
      <li>
        <strong>Device Name:</strong> <%= devices[i].device_name %><br>
        <strong>Phone Number:</strong> <%= devices[i].phone_number %><br>
        <strong>Email:</strong> <%= devices[i].email %><br>
        <% if (devices[i].is_online) { %>
          <p><em><%= devices[i].device_name %> is online.</em></p>
        <% } else { %>
          <p><em><%= devices[i].device_name %> is offline.</em></p>
        <% } %>
        <% if (devices[i].is_heating) { %>
          <p><em>Device is heating.</em></p>
        <% } else { %>
          <p><em>Device is not heating.</em></p>
        <% } %>
      </li>
      <hr>
    <% } %>
  </ul>
</body>
</html>

